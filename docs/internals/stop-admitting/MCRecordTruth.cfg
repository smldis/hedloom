\* The repair: the attempt record decides both whether the work ran and what
\* it produced. The client's futures are not consulted for anything the
\* cancel destroyed. Expected: every invariant holds.
SPECIFICATION Spec
CONSTANTS
    Tasks = {t1, t2, t3}
    ExecutingTest = "call-stack"
    PreserveInFlight = TRUE
    BlockedFromRecord = TRUE
    OutcomeFromRecord = TRUE
INVARIANT TypeOK
INVARIANT ReportIsTotal
INVARIANT TruthfulOutcomes
INVARIANT NoQueuedWorkSurvivesTheStop
INVARIANT BlockedNeverRan
CHECK_DEADLOCK TRUE
