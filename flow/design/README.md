# Hedloom Flow design record — working material, not documentation

Not published: `composition.py docs` stages only `flow/docs/`. These files
record arguments made on a date about a tree that has moved since, and are not
maintained against the code.

| File | What it is | Status |
| --- | --- | --- |
| `sequential-flow-convenience.md` | The design for a sequential convenience layer over the planning boundary. | Inactive. Not a backlog item — `hedloom`'s `submit` occupies this space now. |

The unit's live development trackers are `../PLANNING.md` and
`../IMPLEMENTATION.md`, which are also unpublished and also historical: their
phase records describe what each phase delivered, including contracts that have
since changed (schema 2, for instance, where the unit now emits and validates
schema 3 only). Read them as a record of the past. The current contracts are in
`../ONTOLOME.md`, and the current architecture is in `../docs/architecture.md`.
